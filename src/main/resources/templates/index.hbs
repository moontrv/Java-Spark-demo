{{#partial "content"}}
    {{#if username}}
        <h1>Welcome {{username}}</h1>
        <p>View <a href="/ideas"</p> all course ideas.</p>
    {{else}}
        <h1>Welcome User!</h1>
        <form action="/sign-in" method="post">
            <input type="text" placeholder="What's your name?" name="username" />
            <button>Sign in</button>
        </form>
    {{/if}}
{{/partial}}

{{> base.hbs}}